#pragma once

#include "lvgl.h"

// autogenerated, do not modify!

class Theme {
  protected:
    {% for screen in screens %}
    const lv_img_dsc_t *ui_img_{{ screen }};
    {% endfor %}

  public:
    // Getters
    {% for screen in screens %}
    const lv_img_dsc_t *get{{ screen.capitalize() }}() const {
        return ui_img_{{ screen }};
    }
    {% endfor %}

    // Setters
    {% for screen in screens %}
    void set{{ screen.capitalize() }}(const lv_img_dsc_t *img) {
        ui_img_{{ screen }} = img;
    }
    {% endfor %}
};

{% for theme in themes %}
// image files for theme {{ theme }}
{% for screen in screens %}
LV_IMG_DECLARE(ui_img_{{ theme }}_{{ screen }}_png);
{% endfor %}

{% endfor %}

{% for theme in themes %}
// class  for theme {{ theme }}
class Theme{{ theme.capitalize() }} : public Theme {
public:
    Theme{{ theme.capitalize() }}() {
        {% for screen in screens %}
        set{{ screen.capitalize() }}(&ui_img_{{ theme }}_{{ screen }}_png);
        {% endfor %}
    }
};

{% endfor %}

